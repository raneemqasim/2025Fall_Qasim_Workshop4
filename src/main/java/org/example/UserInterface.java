package org.example;

public class UserInterface {
    //is this a property? is that what the diagram is saying?
    private Dealership dealership;

    //UserInterface();
    //display();
    /*
    the display() method. At the top of display(), call a private init() not
shown in the diagram above. It will load the dealership. Then create a
loop and within it 1) display the menu, 2) read the user's command, and 3)
code a switch statement that calls the correct process() method that
matches the user request. (Note: If you want to create a helper method to
display the menu, that would be a good idea!)
     */


    //processGetByPriceRequest();
    //processGetByMakeModelRequest();
    //processGetByYearRequest();
    //processGetByColorRequest();
    //processGetByMileageRequest();
    //processGetByVehicleTypeRequest();
    //processGetAllVehiclesRequest();
    //processAddVehicleRequest();
    //processRemoveVehicleRequest();


}
